FROM node:18-alpine
WORKDIR /app

COPY package*.json ./
RUN if [ -f package.json ]; then npm ci --omit=dev || npm install --omit=dev; fi

COPY . .

ENV PORT=3000
EXPOSE 3000

CMD ["node", "index.js"]
